In the chatbot creation,
	1.For the collection of data(python(version 3.7) or anaconda3)
		Install the package(in prompt) by pip(python install packages)
		 -> pip install chatterbor==0.8.6
	2.other are mentioned in ANN.py
for terminating the chatbot the word "bye" is used.